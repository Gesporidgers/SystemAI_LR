<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="SystemAI_LR.LR2"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SystemAI_LR"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <SplitView IsPaneOpen="True" OpenPaneLength="300" DisplayMode="Inline">
        <SplitView.Pane>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="250"/>
                    <ColumnDefinition Width=".25*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height=".5*"/>
                    <RowDefinition Height=".5*"/>
                    <RowDefinition />
                    <RowDefinition Height=".25*"/>
                    <RowDefinition />
                </Grid.RowDefinitions>
                <Button Grid.Column="0" Grid.Row="0" Width="225" HorizontalAlignment="Center" Content="Выбрать изображение сцены" Click="Button_ClickAsync" x:Name="OpenScene"/>
                <SymbolIcon x:Name="iconImgStatus1" Symbol="Accept" Grid.Column="1" Grid.Row="0" Foreground="Lime" Visibility="Collapsed"/>
                <SymbolIcon x:Name="iconImgStatus2" Symbol="Accept" Grid.Column="1" Grid.Row="1" Foreground="Lime" Visibility="Collapsed"/>
                <Button Grid.Column="0" Grid.Row="1" Width="225" HorizontalAlignment="Center" Content="Выбрать изображение объекта" Click="Button_ClickAsync" x:Name="OpenObject"/>
                
                <Button Grid.Row="4" Grid.ColumnSpan="2" Content="Обработать" HorizontalAlignment="Center" x:Name="ProcessFrame" Click="ProcessFrame_Click"/>
            </Grid>

        </SplitView.Pane>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition/>
            </Grid.RowDefinitions>
            <ProgressBar Visibility="Collapsed" Padding="10" x:Name="prog" Maximum="13"/>
            <Border BorderBrush="Gray">
                <Image Height="800" x:Name="ResultImageS"/>
            </Border>
        </Grid>
        
    </SplitView>
</UserControl>
