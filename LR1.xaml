<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="SystemAI_LR.LR1"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SystemAI_LR"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    >

    <StackPanel Padding="10" Spacing="10">
        <Grid ColumnSpacing="4">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Border BorderThickness="1" BorderBrush="Gray" CornerRadius="4">
                <Image Height="600" x:Name="sourceImg"/>
            </Border>
            <Border Grid.Column="1" BorderThickness="1" BorderBrush="Gray" CornerRadius="4">
                <Image Height="600"/>
            </Border>
        </Grid>
        <StackPanel Orientation="Horizontal">
            <Button Content="Выбрать изображение" Click="Button_Click"/>
            <Button Content="Detect" Click="Button_Click_1"/>
        </StackPanel>
        
    </StackPanel>
</UserControl>
